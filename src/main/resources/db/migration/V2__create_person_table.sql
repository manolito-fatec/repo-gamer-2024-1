CREATE TABLE person (
    id_person NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    ito_person_code VARCHAR2(36) UNIQUE NOT NULL,
    name VARCHAR2(120) NOT NULL,
    device_code VARCHAR2(36) NOT NULL
);